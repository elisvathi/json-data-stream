import { ArrayChunkCodec } from '../../codecs/ArrayChunkCodec';

// export class ArrayReadableStream<T> extends ObjectReadStream<T[], T[]> {
//   constructor(message_count: number) {
//     super(new ArrayChunkCodec<T>(), message_count);
//   }
// }
